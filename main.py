# -*- coding: utf-8 -*-
"""
Точка входа для облака.

Amvera запускает файл, а не модуль, а `python bot/main.py` сломается на
относительных импортах внутри пакета. Этот файл лежит в корне, поэтому
пакет bot виден, и запуск сводится к обычному вызову.

Локально по-прежнему работает и `python -m bot.main`.
"""
from bot.main import main

if __name__ == "__main__":
    main()
